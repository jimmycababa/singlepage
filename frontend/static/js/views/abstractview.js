export default class {
    constructor(){

    }
    // updates the title that shows on the tab
    setTitle(title) {
        document.title = title;
    }

    async getHtml(){
        return "";
    }
}